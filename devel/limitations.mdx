---
title: 'Limitations'
description: 'Limitations of the PulsejetDB'
icon: 'ban'
---

* Since we are using `.` internally for block and collection reservations. It is not allowed to use `.` in the collection names.
* `ANODE` index doesn't have TOPK queries more than 70 for now. We are working on to make it configurable.